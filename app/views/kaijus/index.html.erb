<h1>Kaijus</h1>
<ul>
  <% @kaijus.each do |kaiju| %>
    <li> <%= link_to kaiju.name, kaiju_path(kaiju) %> </li>
  <% end %>
</ul>

<h1>Kaiju Works</h1>
<ul>
  <% @works.each do |work| %>
  <li> <%= link_to work.title, work_path(work) %> </li>
  <% end %>
</ul>
